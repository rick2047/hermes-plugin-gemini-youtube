[project]
name = "gemini-youtube-analyzer-hermes-plugin"
version = "0.1.0"
description = "Hermes directory plugin for Gemini-native YouTube analysis"
requires-python = ">=3.11"
dependencies = [
  "google-genai==2.7.0",
]

[tool.uv]
package = false
